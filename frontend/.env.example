# For the team to know which environment variables are needed for the frontend application, this file serves as a template. Copy this file to `.env` and fill in the required values before running the application.
VITE_API_BASE_URL=http://localhost:5000/api